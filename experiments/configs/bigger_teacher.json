{
  "written": "2026-09-19, before any 4B/8B query; user chose option 4 (bigger teacher)",
  "why": "the fixed 2B teacher scores 1.73 on DTC, below PPO-from-scratch at 50K steps (~3.1), so it cannot accelerate RL beyond the first ~50K steps",
  "candidates": {
    "Qwen/Qwen3-VL-4B-Instruct": "fp32, split over 2xT4",
    "Qwen/Qwen3-VL-8B-Instruct": "fp16, split over 2xT4 (fp32 needs ~35 GB > 32 GB)"
  },
  "stage1_small_check": "synthetic red-disc localisation (32 images) must be >= 0.9 accurate with mean answer-letter mass >= 0.9; then the pre-registered debias probe on 150 DTC frames must pass (best of A/B/C argmax-in-set >= uniform + 0.15)",
  "stage2_gate": "30-episode DTC gate with the passing model(s)",
  "stage3_bar": "guided RL proceeds only if the teacher's DTC gate mean return >= 3.0 (~PPO from scratch at 50K steps); otherwise ship the reflex policy + negative teacher finding",
  "addendum_nf4": {
    "written": "2026-09-19, after 4B fp32 missed the probe bar by 0.01 and 8B fp16 (split over 2 GPUs) failed the synthetic check; before any NF4 query",
    "correction": "8B fp16 result confounds fp16 with 2-GPU splitting; not attributed to fp16 alone",
    "candidate": "Qwen/Qwen3-VL-8B-Instruct, 4-bit NF4 weights with fp32 compute, single T4",
    "rule": "same stage1 criteria (synthetic >= 0.9 with letter mass >= 0.9, then probe best-of-A/B/C >= uniform + 0.15), then the >= 3.0 gate bar"
  },
  "stage2_8b_gate": {
    "written": "2026-09-19 after 8B-NF4-fp32 passed stage 1 (synthetic 1.0, letter mass 1.0, probe C 0.66 >= 0.63); before any 8B gate episode",
    "protocol": "DTC, calibrated variant, 30 episodes split 15+15 over two T4s (lane 0 env seeds 50000+, lane 1 50100+); same pass rule vs the 30-episode random baseline, plus the >= 3.0 mean-return bar for guided RL",
    "note": "lane-1 seeds differ from the random baseline's seeds; episodes are exchangeable draws so the Welch test is unaffected"
  }
}